export enum PreferenceTooltipEnum {
  Review_Buffer = "This is the time during which admin has to review the activity.If admin does not review the activity, our system will assume that activity has been reviewed and will send “Accept/reject” notification to driver.",
  Accept_Reject_Buffer = "Time before Accept reject notifications are to be sent to driver",
  Start_Buffer = "Time before “Activity start” notification is sent to driver.",
  Activity_Time = "Average time for an activity to be completed",
  Activity_Accept_Driver_Buffer = "Time during which driver has to either accept or reject the activity.",
  Activity_Start_Driver_Buffer = "Time during which driver has to either start or abort the activity.",
}
